import Button, { ButtonProps } from './Button';
import {
  ButtonStyle,
  ButtonType,
} from './enums';

export {
  ButtonStyle,
  ButtonType,
};

export type { ButtonProps };

export default Button;
